# Tbxark Repo

## How do I install these formulae?
`brew install tbxark/repo/<formula>`

Or `brew tap tbxark/repo` and then `brew install <formula>`.

## Documentation
`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
